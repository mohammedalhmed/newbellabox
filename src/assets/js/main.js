document.addEventListener('DOMContentLoaded',function(){
  var nav=document.querySelector('.mobile-nav'), overlay=document.querySelector('.mobile-nav-overlay'), toggle=document.querySelector('.mobile-menu-toggle');
  function closeMenu(){if(!nav)return;nav.classList.remove('is-open');overlay&&overlay.classList.remove('is-open');toggle&&toggle.setAttribute('aria-expanded','false');nav.setAttribute('aria-hidden','true');document.body.classList.remove('menu-open')}
  if(toggle&&nav){toggle.addEventListener('click',function(){var open=!nav.classList.contains('is-open');nav.classList.toggle('is-open',open);overlay&&overlay.classList.toggle('is-open',open);toggle.setAttribute('aria-expanded',String(open));nav.setAttribute('aria-hidden',String(!open));document.body.classList.toggle('menu-open',open)});document.querySelectorAll('[data-mobile-close]').forEach(function(el){el.addEventListener('click',closeMenu)});nav.querySelectorAll('a').forEach(function(a){a.addEventListener('click',closeMenu)})}
  document.querySelectorAll('.search-trigger').forEach(function(button){button.addEventListener('click',function(){if(window.salla&&salla.event){salla.event.dispatch('search::open')}else{var input=document.querySelector('.header-search-input');input&&input.focus()}})});
  var top=document.querySelector('.back-to-top');if(top){window.addEventListener('scroll',function(){top.classList.toggle('is-visible',window.scrollY>500)},{passive:true});top.addEventListener('click',function(){window.scrollTo({top:0,behavior:'smooth'})})}
  document.querySelectorAll('[data-wishlist-id]').forEach(function(btn){btn.addEventListener('click',function(){btn.classList.toggle('active')})});
  document.querySelectorAll('a[href="#"]').forEach(function(a){a.addEventListener('click',function(e){e.preventDefault()})});
});
window.BellaBox={subscribeNewsletter:function(event){event.preventDefault();var form=event.currentTarget;var button=form.querySelector('button');var original=button.innerHTML;button.innerHTML='تم الاشتراك <i class="sicon-check"></i>';button.disabled=true;setTimeout(function(){button.innerHTML=original;button.disabled=false;form.reset()},2600);return false;}};
